/**
 * Implemen function that takes two numbers and returns the sum of this two numbers.
 * Implement unit tests for this function - test should cover all requirements below.
 *  - If any of arguments is not a number - return message "Incorrect arguments type" and value of argument.
 *  - If argument a String that can be casting to a number ("123") it should be caster to number.
 *  - If there is only 1 argument instead of 2 - return message "Second argument is needed.
 *  - If there are more than 2 arguments - return message "There are more than 2 arguments"
 * @param a
 * @param b
 * @returns {undefined}
 */
let addition = function (a, b) {
    return 0;
};

/**
 * Implement method that returns the lowest argument
 * @param a
 * @param b
 * @param c
 */
let min = function(a, b, c) {
    return 0;
};

module.exports = addition;